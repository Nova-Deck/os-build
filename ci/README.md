# ci/

GitHub Actions build of the single unified image (covers SM8550 / SM8650 / SM8750 in one
kernel + boot artifact) + cosign-signed artifacts + published update manifests. CI patterns
borrowed from ublue-os/image-template; runtime stays SteamOS-native (RAUC/casync/Btrfs).

_Phase 0 placeholder — populated in Phase 7._
