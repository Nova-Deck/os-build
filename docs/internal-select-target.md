# select-target hardware run — AYANEO Pocket ACE

Generated by `install/hw-select-target.sh` — read-only Phase 3 validation.

| | |
|---|---|
| Ran | 2026-08-21T07:54:47Z |
| Board | `AYANEO Pocket ACE` |
| Kernel | `7.2.0` |
| sgdisk | `GPT fdisk (sgdisk) version 1.0.10` |
| NOVADECK_VARIANT | `unified` |
| NOVADECK_MODE | `dev` |
| NOVADECK_VERSION | `dev` |
| NOVADECK_BUILD | `20260821T073416Z` |
| NOVADECK_GIT | `71fa989` |
| Running root | `/dev/mmcblk0p4` |
| Boot medium — never a target | `mmcblk0` |

## Logical / physical sector size per disk

| Disk | logical | physical | size |
|---|---|---|---|
| `mmcblk0` | 512 | 512 | 28.8G |
| `sda` | 4096 | 4096 | 115G |
| `sdb` | 4096 | 4096 | 20M |
| `sdc` | 4096 | 4096 | 20M |
| `sdd` | 4096 | 4096 | 32M |
| `sde` | 4096 | 4096 | 4G |
| `sdf` | 4096 | 4096 | 32M |

## The scan — no argument

Rules 1, 2 and 9 reach this path and no other: the explicit-target form below cannot pick a
disk, so it cannot exercise "never pick when two are eligible" either.

```
$ select-target.sh 
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
TARGET=/dev/sda
MODE=fresh
SECTOR=4096
UD_INDEX=11
UD_START=4794920
UD_END=30150650
UD_TYPE=1B81E7E6-F50D-419B-A739-2AEEF8DA3335
CEIL=30150650
rc=0
```

## Every disk, named explicitly

### `/dev/sda` — 115G

PARTLABELs: `ssd persist misc keystore frp super vbmeta_system_a vbmeta_system_b metadata rawdump userdata `

```
$ select-target.sh /dev/sda
TARGET=/dev/sda
MODE=fresh
SECTOR=4096
UD_INDEX=11
UD_START=4794920
UD_END=30150650
UD_TYPE=1B81E7E6-F50D-419B-A739-2AEEF8DA3335
CEIL=30150650
rc=0
```

### `/dev/sdb` — 20M

PARTLABELs: `xbl_a xbl_config_a multiimgqti_a multiimgoem_a apdp `

```
$ select-target.sh /dev/sdb
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdb is not a valid target
rc=1
```

### `/dev/sdc` — 20M

PARTLABELs: `xbl_b xbl_config_b multiimgqti_b multiimgoem_b apdpb `

```
$ select-target.sh /dev/sdc
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdc is not a valid target
rc=1
```

### `/dev/sdd` — 32M

PARTLABELs: `ALIGN_TO_128K_1 cdt ddr `

```
$ select-target.sh /dev/sdd
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdd is not a valid target
rc=1
```

### `/dev/sde` — 4G

PARTLABELs: `uefi_a aop_a aop_config_a tz_a hyp_a modem_a bluetooth_a mdtpsecapp_a mdtp_a abl_a dsp_a keymaster_a boot_a devcfg_a qupfw_a vbmeta_a dtbo_a uefisecapp_a imagefv_a shrm_a vm-bootsys_a cpucp_a featenabler_a vendor_boot_a qmcs qweslicstore_a recovery_a xbl_ramdump_a rticmpdata_a init_boot_a uefi_b aop_b aop_config_b tz_b hyp_b modem_b bluetooth_b mdtpsecapp_b mdtp_b abl_b dsp_b keymaster_b boot_b devcfg_b qupfw_b vbmeta_b dtbo_b uefisecapp_b imagefv_b shrm_b vm-bootsys_b cpucp_b featenabler_b vendor_boot_b qweslicstore_b recovery_b xbl_ramdump_b rticmpdata_b init_boot_b devinfo dip splash limits limits-cdsp toolsfv logfs logdump storsec uefivarstore secdata vm-data vm-persist mdcompress connsec tzsc spunvm xbl_sc_test_mode xbl_sc_logs `

```
$ select-target.sh /dev/sde
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sde is not a valid target
rc=1
```

### `/dev/sdf` — 32M

PARTLABELs: `ALIGN_TO_128K_2 modemst1 modemst2 fsg fsc `

```
$ select-target.sh /dev/sdf
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdf is not a valid target
rc=1
```

### `/dev/mmcblk0` — 28.8G

PARTLABELs: `NOVADECK-ESP novadeck-efi-A novadeck-efi-B novadeck-root-A novadeck-root-B novadeck-var-A novadeck-var-B novadeck-home `

```
$ select-target.sh /dev/mmcblk0
select-target: /dev/mmcblk0 is the disk the running system is on
rc=1
```


---

# select-target hardware run — KONKR Pocket FIT

Generated by `install/hw-select-target.sh` — read-only Phase 3 validation.

| | |
|---|---|
| Ran | 2026-08-21T08:04:09Z |
| Board | `KONKR Pocket FIT` |
| Kernel | `7.2.0` |
| sgdisk | `GPT fdisk (sgdisk) version 1.0.10` |
| mdir | `mdir (GNU mtools) 4.0.49` |
| NOVADECK_VARIANT | `unified` |
| NOVADECK_MODE | `dev` |
| NOVADECK_VERSION | `dev` |
| NOVADECK_BUILD | `20260821T073416Z` |
| NOVADECK_GIT | `71fa989` |
| Running root | `/dev/mmcblk0p4` |
| Boot medium — never a target | `mmcblk0` |

## Logical / physical sector size per disk

| Disk | logical | physical | size |
|---|---|---|---|
| `mmcblk0` | 512 | 512 | 28.8G |
| `sda` | 4096 | 4096 | 464.5G |
| `sdb` | 4096 | 4096 | 20M |
| `sdc` | 4096 | 4096 | 20M |
| `sdd` | 4096 | 4096 | 32M |
| `sde` | 4096 | 4096 | 4G |
| `sdf` | 4096 | 4096 | 32M |
| `sdg` | 4096 | 4096 | 4G |
| `sdh` | 4096 | 4096 | 4G |

## The scan — no argument

Rules 1, 2 and 9 reach this path and no other: the explicit-target form below cannot pick a
disk, so it cannot exercise "never pick when two are eligible" either.

```
$ select-target.sh 
  /dev/sda: partition 12 (ROCKNIX) is a bootable ESP that is not ours -- remove the other OS first
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
  /dev/sdg: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
  /dev/sdh: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
select-target: no disk qualifies -- see the reasons above
rc=1
```

## Every disk, named explicitly

### `/dev/sda` — 464.5G

PARTLABELs: `ssd persist misc keystore frp super vbmeta_system_a vbmeta_system_b metadata rawdump userdata ROCKNIX STORAGE `

```
$ select-target.sh /dev/sda
  /dev/sda: partition 12 (ROCKNIX) is a bootable ESP that is not ours -- remove the other OS first
select-target: /dev/sda is not a valid target
rc=1
```

### `/dev/sdb` — 20M

PARTLABELs: `xbl_a xbl_config_a multiimgqti_a multiimgoem_a apdp `

```
$ select-target.sh /dev/sdb
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdb is not a valid target
rc=1
```

### `/dev/sdc` — 20M

PARTLABELs: `xbl_b xbl_config_b multiimgqti_b multiimgoem_b apdpb `

```
$ select-target.sh /dev/sdc
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdc is not a valid target
rc=1
```

### `/dev/sdd` — 32M

PARTLABELs: `ALIGN_TO_128K_1 cdt ddr `

```
$ select-target.sh /dev/sdd
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdd is not a valid target
rc=1
```

### `/dev/sde` — 4G

PARTLABELs: `uefi_a aop_a aop_config_a tz_a hyp_a modem_a bluetooth_a abl_a dsp_a keymaster_a boot_a devcfg_a qupfw_a vbmeta_a dtbo_a uefisecapp_a imagefv_a shrm_a vm-bootsys_a cpucp_a featenabler_a vendor_boot_a qmcs qweslicstore_a recovery_a xbl_ramdump_a init_boot_a cpucp_dtb_a pvmfw_a spuservice_a uefi_b aop_b aop_config_b tz_b hyp_b modem_b bluetooth_b abl_b dsp_b keymaster_b boot_b devcfg_b qupfw_b vbmeta_b dtbo_b uefisecapp_b imagefv_b shrm_b vm-bootsys_b cpucp_b featenabler_b vendor_boot_b qweslicstore_b recovery_b xbl_ramdump_b init_boot_b cpucp_dtb_b pvmfw_b spuservice_b devinfo toolsfv logfs logdump storsec uefivarstore secdata vm-persist mdcompress connsec tzsc spunvm xbl_sc_test_mode xbl_sc_logs dpm `

```
$ select-target.sh /dev/sde
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sde is not a valid target
rc=1
```

### `/dev/sdf` — 32M

PARTLABELs: `ALIGN_TO_128K_2 modemst1 modemst2 fsg fsc `

```
$ select-target.sh /dev/sdf
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdf is not a valid target
rc=1
```

### `/dev/sdg` — 4G

PARTLABELs: ``

```
$ select-target.sh /dev/sdg
  /dev/sdg: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
select-target: /dev/sdg is not a valid target
rc=1
```

### `/dev/sdh` — 4G

PARTLABELs: ``

```
$ select-target.sh /dev/sdh
  /dev/sdh: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
select-target: /dev/sdh is not a valid target
rc=1
```

### `/dev/mmcblk0` — 28.8G

PARTLABELs: `NOVADECK-ESP novadeck-efi-A novadeck-efi-B novadeck-root-A novadeck-root-B novadeck-var-A novadeck-var-B novadeck-home `

```
$ select-target.sh /dev/mmcblk0
select-target: /dev/mmcblk0 is the disk the running system is on
rc=1
```


---

# select-target hardware run — MANGMI Pocket Max

Generated by `install/hw-select-target.sh` — read-only Phase 3 validation.

| | |
|---|---|
| Ran | 2026-08-21T09:43:53Z |
| Board | `MANGMI Pocket Max` |
| Kernel | `7.2.0` |
| sgdisk | `GPT fdisk (sgdisk) version 1.0.10` |
| mdir | `mdir (GNU mtools) 4.0.49` |
| NOVADECK_VARIANT | `unified` |
| NOVADECK_MODE | `dev` |
| NOVADECK_VERSION | `dev` |
| NOVADECK_BUILD | `20260821T073416Z` |
| NOVADECK_GIT | `71fa989` |
| Running root | `/dev/mmcblk0p4` |
| Boot medium — never a target | `mmcblk0` |

## Logical / physical sector size per disk

| Disk | logical | physical | size |
|---|---|---|---|
| `mmcblk0` | 512 | 512 | 28.8G |
| `sda` | 4096 | 4096 | 105.1G |
| `sdb` | 4096 | 4096 | 8M |
| `sdc` | 4096 | 4096 | 8M |
| `sdd` | 4096 | 4096 | 32M |
| `sde` | 4096 | 4096 | 6G |
| `sdf` | 4096 | 4096 | 32M |
| `sdg` | 4096 | 4096 | 4G |
| `sdh` | 4096 | 4096 | 4G |

## The scan — no argument

Rules 1, 2 and 9 reach this path and no other: the explicit-target form below cannot pick a
disk, so it cannot exercise "never pick when two are eligible" either.

```
$ select-target.sh 
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
  /dev/sdg: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
  /dev/sdh: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
TARGET=/dev/sda
MODE=fresh
SECTOR=4096
UD_INDEX=15
UD_START=1735208
UD_END=27540474
UD_TYPE=1B81E7E6-F50D-419B-A739-2AEEF8DA3335
CEIL=27540474
rc=0
```

## Every disk, named explicitly

### `/dev/sda` — 105.1G

PARTLABELs: `ssd persist misc keystore frp super recovery_a recovery_b vbmeta_system_a vbmeta_system_b metadata vm-system_a vm-system_b rawdump userdata `

```
$ select-target.sh /dev/sda
TARGET=/dev/sda
MODE=fresh
SECTOR=4096
UD_INDEX=15
UD_START=1735208
UD_END=27540474
UD_TYPE=1B81E7E6-F50D-419B-A739-2AEEF8DA3335
CEIL=27540474
rc=0
```

### `/dev/sdb` — 8M

PARTLABELs: `xbl_a xbl_config_a `

```
$ select-target.sh /dev/sdb
  /dev/sdb: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdb is not a valid target
rc=1
```

### `/dev/sdc` — 8M

PARTLABELs: `xbl_b xbl_config_b `

```
$ select-target.sh /dev/sdc
  /dev/sdc: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdc is not a valid target
rc=1
```

### `/dev/sdd` — 32M

PARTLABELs: `ALIGN_TO_128K_1 cdt ddr mdmddr `

```
$ select-target.sh /dev/sdd
  /dev/sdd: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdd is not a valid target
rc=1
```

### `/dev/sde` — 6G

PARTLABELs: `aop_a tz_a hyp_a modem_a bluetooth_a mdtpsecapp_a mdtp_a abl_a dsp_a keymaster_a boot_a cmnlib_a cmnlib64_a devcfg_a qupfw_a vbmeta_a dtbo_a uefisecapp_a multiimgoem_a multiimgqti_a vm-linux_a featenabler_a imagefv_a aop_b tz_b hyp_b modem_b bluetooth_b mdtpsecapp_b mdtp_b abl_b dsp_b keymaster_b boot_b cmnlib_b cmnlib64_b devcfg_b qupfw_b vbmeta_b dtbo_b uefisecapp_b multiimgoem_b multiimgqti_b vm-linux_b featenabler_b imagefv_b devinfo dip apdp msadp spunvm limits limits-cdsp logfs logdump storsec uefivarstore secdata vm-keystore vm-data `

```
$ select-target.sh /dev/sde
  /dev/sde: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sde is not a valid target
rc=1
```

### `/dev/sdf` — 32M

PARTLABELs: `ALIGN_TO_128K_2 modemst1 modemst2 fsg fsc mdm1m9kefs3 mdm1m9kefs1 mdm1m9kefs2 mdm1m9kefsc `

```
$ select-target.sh /dev/sdf
  /dev/sdf: no partition named 'userdata' -- this board is not supported yet
select-target: /dev/sdf is not a valid target
rc=1
```

### `/dev/sdg` — 4G

PARTLABELs: ``

```
$ select-target.sh /dev/sdg
  /dev/sdg: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
select-target: /dev/sdg is not a valid target
rc=1
```

### `/dev/sdh` — 4G

PARTLABELs: ``

```
$ select-target.sh /dev/sdh
  /dev/sdh: the GPT is damaged or absent -- sgdisk said: Creating new GPT entries in memory.
select-target: /dev/sdh is not a valid target
rc=1
```

### `/dev/mmcblk0` — 28.8G

PARTLABELs: `NOVADECK-ESP novadeck-efi-A novadeck-efi-B novadeck-root-A novadeck-root-B novadeck-var-A novadeck-var-B novadeck-home `

```
$ select-target.sh /dev/mmcblk0
select-target: /dev/mmcblk0 is the disk the running system is on
rc=1
```

