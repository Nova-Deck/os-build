# firmware/

Vendor firmware **extraction and bundling recipes only**. Proprietary Qualcomm
blobs are NEVER committed (see `.gitignore`: `blobs/`, `extracted/`). Recipes pull
DSP/GPU-zap/etc. from the user's own device partitions.

_Phase 0 placeholder — populated in Phase 1._
