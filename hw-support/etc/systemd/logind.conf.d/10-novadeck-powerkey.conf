# novadeck: the power key is owned by novadeck-waked (suspend/resume), not logind. Tell logind to
# leave the short press alone so it does not poweroff. A LONG press still triggers the PMIC hardware
# power-off (independent of logind), which preserves a hard power-off escape.
[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
