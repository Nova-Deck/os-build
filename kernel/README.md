# kernel/

Kernel config, patches, and per-SoC build. Mainline/Linaro `sm8x50` base with
vendor cherry-picks. Page-size decision (4K vs 16K) is made here — it affects
FEX/Proton/Wine.

_Phase 0 placeholder — populated in Phase 1._
