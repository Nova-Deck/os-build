# kernel/patches/

Out-of-tree kernel patches applied on top of the pinned source by `kernel/build.sh`.

Apply order is lexical (`0001-*.patch`, `0002-*.patch`, …). Keep each patch focused
and traceable to an upstream submission or a clear bring-up reason.

_Phase 1 scaffold — empty until SM8650 bring-up surfaces gaps in the pinned tree._
