# Raise the minimum clock quantum: the SM8650 AudioReach/q6apm path underruns at
# tiny buffers, producing crackle. 512 samples @ 48kHz (~10.7ms) is a stable floor.
context.properties = {
    default.clock.min-quantum = 512
}
